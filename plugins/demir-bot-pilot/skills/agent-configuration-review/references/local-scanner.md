# Optional local scanner

Resolve the active private profile through the installed demir-bot skill's references/profile-loading.md. Read `local-scanner.md` and local-setup.md there only for a requested operation. They contain host-specific installation evidence and commands, not distributable runtime code. If absent, use supported available capabilities or report the missing setup; do not copy another user's paths/account or install a runtime automatically.

Run a scanner only when explicitly requested, with a scoped target and inspected dependency/data-flow behavior. No default scan, fix mode, external-model call or background service.

Source provenance remains in the private setup record and existing upstream-adaptations.md; resolve original sources before redistribution of executable code.
