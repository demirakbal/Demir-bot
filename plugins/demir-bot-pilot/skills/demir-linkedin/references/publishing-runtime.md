# Optional local publisher

Resolve the active private profile through the bundled demir-bot-pilot:demir-bot skill's references/profile-loading.md. Read `publishing-runtime.md` and local-setup.md there only for a requested operation. They contain host-specific installation evidence and commands, not distributable runtime code. If absent, use supported available capabilities or report the missing setup; do not copy another user's paths/account or install a runtime automatically.

For publishing, preserve exact-action approval, immutable payloads, held uncertain attempts and the trusted-local-caller limitation. Follow actions.md and approval-ledger.md. Installation and account reads never establish publication; no exactly-once delivery guarantee.

Source provenance remains in the private setup record and existing upstream-adaptations.md; resolve original sources before redistribution of executable code.
