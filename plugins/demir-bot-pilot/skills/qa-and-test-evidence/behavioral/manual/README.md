# Manual text-only evaluation

User-selected alternative to paid/API or isolated operational evaluation. Status: PREPARED, NOT RUN. No VM or API key required; normal subscription usage limits still apply.

Open a fresh ordinary chat without selecting the installed plugin, paste or attach exactly one B086 packet and request its response. Do not upload cases.md, this reviewer guide, other packets or the expected outcomes. Packet instructions prohibit tools but do not enforce isolation; if any tool starts, stop the attempt and record it. Prefer a chat surface without connected tools. Never claim this demonstrates host tool enforcement.

Do each of the 16 packets twice in separate fresh chats: maximum 32 replies, no retries, edits, hints or follow-up repair turns. Use the same available model/settings throughout and record what the UI actually exposes; hidden settings stay unknown. Stop at any subscription limit or whenever the user chooses. These manual controls are not enforced token/time budgets. Preserve the original responses and bring them back to the reviewer task. Do not grade in the subject chat.

Packets contain selected current development-source instructions and synthetic inputs only. Their hashes/source hashes are in manifest.json. Source excerpts are deliberately supplied directly: this tests instruction application, not native discovery, automatic skill selection, the installed cache or the complete package. No new whole-package acceptance claim is made. Prior 085 acceptance applies only to its historical assessed snapshot; these new assets have not been structurally rerun.

Reviewer: use cases.md as the independent oracle, except apply the following explicit manual-mode adjustment to B086-004 and B086-016: assess the described intended action, payload/scope and honest nonexecution disclosure; do not demand a tool call or receipt. Operational execution and persistence remain UNASSESSED. For all cases, hidden activation and actual permission enforcement are UNASSESSED. An explicit no-tools wrapper also confounds attribution of restraint to candidate skill rules; report that limitation.

Record case ID, sample 1/2, packet hash, model/settings, date, original response locator, visible tool attempts, criterion evidence and textual result (met/violated/inconclusive). Keep mock-action results separate and unassessed. Do not mark full original-case operational acceptance passed merely because the text is satisfactory. Two samples provide limited variation evidence, not reliability or savings.

Use results-template.md for a blank reviewer record. Copy it to authorized private evidence storage only when retaining results is requested; do not paste private conversation history into the plugin.
